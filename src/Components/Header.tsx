import React from 'react'

import { AppBar, Toolbar, Typography } from '@mui/material'

export const Header = () => {
  return (
      <AppBar position='sticky'>
          <Toolbar>
              <Typography>R Lopxhan</Typography>
              
        </Toolbar>
    </AppBar>
  )
}
